import 'package:flutter/material.dart';

class ScreenUserInfo extends StatelessWidget{
  const ScreenUserInfo({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context){
    return Scaffold(
      body:Navigator(
        //pushNamedAndRemoveUntil(context, '/', (_) => false);
      )
    );
  }
}